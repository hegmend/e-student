[tool.poetry]
name = "e-student"
version = "0.1.0"
description = ""
authors = ["Your Name <email@example.com>"]
packages = [{include = "*"}]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
